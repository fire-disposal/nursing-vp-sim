# ========================================
# 本地开发环境变量 (.env.example)
# 复制为 .env 并填入实际值
# ========================================

# ── 必填 ────────────────────────────────
ENV=development

# 数据库
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vptest

# JWT 密钥 + API Key 加密派生
# 使用 python -c "import secrets; print(secrets.token_urlsafe(32))" 生成
SECRET_KEY=change-me-to-a-random-secret-key

# DeepSeek API Key（首次启动自动 seed 到数据库，之后在管理面板管理）
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost


# ── Docker 部署 ──────────────────────────
# docker-compose.yml 自动注入 DATABASE_URL (db:5432)
# cd.yml 自动生成 SECRET_KEY 和 POSTGRES_PASSWORD

POSTGRES_PASSWORD=postgres


# ── 可选调参（均有良好默认值）──────────────
# LLM_CONCURRENT_LIMIT=50
# LLM_MAX_RETRIES=3
# ACCESS_TOKEN_EXPIRE_MINUTES=480
