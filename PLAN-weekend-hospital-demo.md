# 周末冲刺计划：医院演示备战

> 目标：下周三前交付可供医院初步功能展示的稳定版本
> 原则：务实的体验优化，不做架构重构，聚焦演示现场可能被关注的环节
> 预估工时：2 天（周六 + 周日）

---

## 战场分析：医院的观众在看什么？

```
┌─────────────────────────────────────────────────────┐
│                  医院演示场景                         │
├─────────────────────────────────────────────────────┤
│                                                      │
│  👨‍⚕️ 护理部主任 → 关心：能不能提升教学效率             │
│     关注点：学生怎么练？练了怎么评？                   │
│                                                      │
│  👩‍🏫 临床带教老师 → 关心：和自己教学流程是否匹配        │
│     关注点：病例真实感、问诊流程完整性                   │
│                                                      │
│  🖥️ IT/信息科 → 关心：系统是否稳定成熟                  │
│     关注点：界面是否专业、操作是否流畅                    │
│                                                      │
└─────────────────────────────────────────────────────┘
```

**关键洞察**：演示不是功能列表过目，而是让观众在 5-10 秒内理解"这个东西对我有用"。

---

## 一、P0 — 演示流程保障（不可妥协）

| # | 事项 | 现状问题 | 修复方案 | 估时 |
|---|---|---|---|---|
| 1 | **演示病例预配置** | 当前唯一起始点是通过 `/cases` 页面选择病例 | 新增一个"演示模式"入口：一键加载最佳展示病例，预置好的 feature flag 组合（开启 emotion + physical_exam + initiative） | 2h |
| 2 | **冷启动体验** | 首次加载时无耐心提示，若 LLM 响应慢会空白 | 在患者回复等待期间显示"患者正在思考..."微动效 | 1h |
| 3 | **评分结果展示** | ScoreCard 弹出后无操作指引，关闭回到 `window.location.href` 硬跳转；动画简陋 | 美化评分卡片过渡动画，关闭后优雅导航回演示入口，增加"重新开始"按钮 | 2h |
| 4 | **错误防护** | 插件 PluginErrorBoundary 只是隐藏组件，报错时用户不明所以 | 全局错误边界，友好提示 + 提示返回入口 | 1h |

### 1.1 演示病例预配置（关键）

在 `frontend/src/pages/` 下新增一个简单页面：

```tsx
// DemoStarter.tsx — 单页入口，点击即开始
// 1. 自动选择"腹痛待查"病例（该病例触发丰富的问诊 + 查体 + 情绪变化）
// 2. 预设 feature flags: { emotion: true, physical_exam: true, patient_initiative: true, questionnaire: false }
// 3. 跳过病例选择列表，直达 training 页面
// 4. 页面底部加"版本信息"的极小字排布

// 在 App.tsx 路由加: /demo → DemoStarter → /training/:recordId (自动创建+跳转)
```

后端新增 `/api/demo/start` 端点：
- 使用预设病例和预设 flag 创建 TrainingRecord
- 返回 record_id 供前端跳转

---

## 二、P1 — 体验亮点打磨（展示价值）

### 2.1 患者情绪可视化

**现状**：EmotionTab 内的 2D 情绪散点图 Canvas 效果不错，但藏在侧栏 Tab 中，默认不展开。

**改进**：
- 当 emotion 功能开启时，在 ChatArea 上方新增一个 **紧凑型情绪指示器**（类似汽车仪表盘）：显示当前情绪标签+小圆点色块
- 每次情绪变化时带短暂高亮动效（`animate-pulse` 1s）
- 让观众在对话进行中就能看见"你的话正在影响患者的情绪"

```tsx
// EmotionIndicator.tsx — 新组件
// 位置：ChatArea 上方（WelcomeScreen 与 ChatDisplay 之间）
// 显示：小尺寸的当前情绪色 + 标签 + trust/comfort 数值
// 行为：在 emotion:changed 事件时闪动 1s
// 如果没有开启 emotion 功能 → 不渲染
```

### 2.2 主动追问可视化

**现状**：InitiativeTab 内显示进度条，但藏在侧栏。

**改进**：
- 当 `patient_initiative` 开启时，在 ChatArea 底部（输入框上方）显示一个细长进度条
- 患者主动发问时，在气泡侧增加"患者主动询问"的小标签
- 让带教老师看到"即使学生不说话，患者也会根据性格主动引导对话"——这恰恰是教学价值所在

### 2.3 特征开关面板优化

**现状**：TrainingHeader 中的"插件特性"弹窗是一个 Modal，支持实时开关。

**改进**：
- 在演示模式下，将特征开关直接显示在 header 中（不用点进 Modal），让观众一目了然看到"你可以开启/关闭这些教学辅助功能"
- 开关显示为紧凑型 Chip + toggle，类似演示者控制面板
- 这是给 IT/信息科看的——展示系统的可配置性

### 2.4 评分结果展示升级

**现状**：ScoreCard 采用固定弹出层，设计简单。

**改进**：
- 加上淡入动画 (`animate-in fade-in zoom-in-95`)
- 评分总分用大字 + 环形进度显示
- 评分维度增加可视化进度条
- 关闭后不需要 `window.location.href` 硬导航，改为 Modal 式的返回逻辑

---

## 三、P2 — 流畅度与稳定性（不出丑）

| # | 事项 | 改进 | 估时 |
|---|---|---|---|
| 1 | **ChatInput 发送后立刻清空** | 当前需等待响应才能再次输入，改为乐观清空+loading 状态 | 0.5h |
| 2 | **消息加载时的骨架屏** | 首次加载消息列表时显示骨架屏而非空白 | 1h |
| 3 | **患者头像在消息中的展示** | 确认所有消息类型都有头像展示（当前 system 消息无头像） | 0.5h |
| 4 | **移动端响应式适配** | PanelHost 的 mobile 模式已大部分实现，补上：竖屏输入框不会被键盘遮挡 | 1.5h |
| 5 | **API 错误提示** | 后端报错时前端有 toast 提示（当前多处 silent catch） | 1h |
| 6 | **特征开关持久化** | 演示开关状态应自动持久化到后端（当前只有核心 flag 被持久化） | 1h |

---

## 四、周末执行排期

### 周六

```
上午 (4h) — 核心流程
  □ P0-1: 演示病例预配置（后端 /api/demo/start + 前端 DemoStarter）
  □ P0-2: 冷启动加载动效（患者思考中...）
  
下午 (4h) — 体验亮点
  □ P1-1: 紧凑型情绪指示器 EmotionIndicator
  □ P1-2: 主动追问的 ChatArea 进度条 + 气泡标签
  □ P2-2: 消息加载骨架屏
```

### 周日

```
上午 (4h) — 评分 + 演示控制
  □ P0-3: 评分结果展示升级（动画 + 环形进度 + 返回导航）
  □ P1-3: 特征开关从 Modal 移动到 Header（演示模式专属）
  
下午 (3h) — 收尾
  □ P0-4: 全局错误边界 + 友好提示
  □ P2-5: API 错误 toast 提示
  □ 冒烟测试：完整走一遍演示流程

预留 1h 缓冲
```

---

## 五、不需要做的事（为了准时交付而放弃）

| 不做 | 原因 |
|---|---|
| 插件系统架构重构（吸收 emotion / physical-exam） | 行为不变，但变更规模大，测试覆盖不足时风险高 |
| 新增病例/API | 演示用已有病例即可 |
| 数据库迁移 | 任何 schema 变更都需要额外验证 |
| 后端路由调整 | 不影响演示质量 |
| 写自动化测试 | 手动冒烟测试就足够覆盖演示路径 |
| 护理记录单的完整填写 | 展示功能存在即可 |

---

## 六、演示当天 checklist

```
开始前：
  □ 打开演示病例（演示模式入口）
  □ 确认 emotion 已开启（患者头像周围有情绪色环）
  □ 确认 physical_exam 已开启（侧栏有查体面板）
  □ 确认 patient_initiative 已开启
  
演示流程：
  1. 展示 WelcomeScreen → 点击快速提问"您好，请问哪里不舒服？"
  2. 展示患者回复 → 情绪指示器显示初始状态 (neutral)
  3. 输入"别担心，慢慢说，我能理解你不舒服" → 情绪向 relaxed 变化（观众可见）
  4. 切换到查体 Tab → 点击"体温" → 看结果注入对话
  5. 让学生沉默 10s → 患者主动追问（观众可见）
  6. 结束训练 → 展示评分报告（环形进度 + 维度 + 建议）
  7. 切换到特征开关 → 关闭 emotion → 重新开始 → 对比有/无情绪差异

备用方案：
  如果 LLM 响应慢 → 准备一组截图/录屏演示素材
  如果网络不行 → 本地运行（后端+前端均可本地化）
```

---

## 七、资源需求

| 资源 | 用途 |
|---|---|
| 一个预设好的"演示病例"（case_data 中有丰富的 exam_anchors + personality 配置） | 后端已有，确认所选病例的配置足够丰富 |
| 几张演示用截图/录屏 | 网络故障时备用 |
| 后端可以本地启动 | 确认本地依赖完整 |
