version: "3.8"

services:
  test-db:
    image: postgres:15
    container_name: nursing-test-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nursing_test
      TZ: Asia/Shanghai
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - test_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d nursing_test"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  test_pg_data:
