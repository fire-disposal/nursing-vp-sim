# 评分功能总体优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 优化评分功能的提示词、解析校验、速度、进度展示和边缘场景兜底

**Architecture:** 5 个模块（A 提示词/B 解析/C 速度/D 进度条/E 兜底），按依赖顺序实施。提示词优化先行（A → B → C 有依赖），进度条和兜底相对独立。

**Tech Stack:** Python/FastAPI/SQLAlchemy + React/TypeScript/TanStack Query

---

## File Structure

| 操作 | 文件 | 模块 | 用途 |
|------|------|------|------|
| 修改 | `backend/prompts/scoring.py` | A1, A3 | 评分/反馈提示词重写 |
| 修改 | `backend/infrastructure/prompt/static.py` | A2 | `build_scoring_criteria` 增加 level 参数 |
| 修改 | `backend/contexts/training/score_engine.py` | D, A2 | tracker 注入 + 使用 brief criteria |
| 修改 | `backend/contexts/training/_scoring_validation.py` | A4, B1 | evidence 校验 + 递归深度限制 |
| 修改 | `backend/infrastructure/llm/parsing.py` | B2 | detail_scores 解析加固 |
| 修改 | `backend/contexts/training/router/scoring.py` | D, E | tracker 生命周期 + progress 返回 |
| 修改 | `backend/core/config.py` | C | SCORING_TIMEOUT 300→180 |
| 修改 | `backend/infrastructure/settlement.py` | E | 超时自动评分 |
| 修改 | `backend/main.py` | D | 安装 ScoringProgressTracker |
| 新建 | `backend/infrastructure/scoring_progress.py` | D | ScoringProgressTracker |
| 修改 | `frontend/src/engine/ScoreManager.ts` | D | 重写 |
| 修改 | `frontend/src/plugins/scoring-display/ScoringOverlay.tsx` | D | 重写 |
| 修改 | `frontend/src/engine/types.ts` | D | 新增 progress 类型 |
| 新建测试 | `backend/tests/test_scoring_progress.py` | D | 进度跟踪器单元测试 |
| 修改 | `backend/tests/test_training.py` | B, D | 补充校验测试 |

---

### Task 1: `_scoring_validation.py` — evidence 校验 + 递归深度限制

**涉及模块：A4, B1**
**测试：`backend/tests/test_training.py`**

- [ ] **Step 1: 在已有校验函数中找到 `_validate_scoring_result`，追加 evidence/reason 校验**

读取 `backend/contexts/training/_scoring_validation.py`，在 `_validate_scoring_result` 末尾（`# ### commands auto generated by` 等无关内容前）追加 `_validate_items_content()` 调用。

```python
def _validate_items_content(detail_scores: dict) -> list[str]:
    errors = []
    for dim_name, dim_data in detail_scores.items():
        if not isinstance(dim_data, dict):
            continue
        for item in dim_data.get("items", []):
            if not isinstance(item, dict):
                continue
            ev = (item.get("evidence") or "").strip()
            rea = (item.get("reason") or "").strip()
            if len(ev) < 10:
                errors.append(f"{dim_name}.{item.get('name','?')}: evidence 过短 ({len(ev)}字)")
            if len(rea) < 5:
                errors.append(f"{dim_name}.{item.get('name','?')}: reason 过短 ({len(rea)}字)")
    return errors
```

然后在 `_validate_scoring_result` 中 `_validate_scoring_essentials(result)` 之后插入：

```python
item_errors = _validate_items_content(result.get("detail_scores", {}))
if item_errors:
    log.warning("评分条目内容校验不通过", extra={"errors": item_errors, "detail_scores": result.get("detail_scores", {})})
```

注意：**不抛出 ValueError**，只打 warning——evidence 过短不应导致整次评分失败，但应记录日志。

- [ ] **Step 2: `_coerce_numeric_fields` 加 depth 限制**

```python
def _coerce_numeric_fields(obj: dict, depth: int = 0):
    if depth > 10:
        log.warning("coerce_numeric_fields 超过最大递归深度 %d", depth)
        return
    for key in ("total_score", "score", "max"):
        if key in obj and isinstance(obj[key], str):
            with suppress(ValueError):
                obj[key] = float(obj[key]) if "." in obj[key] else int(obj[key])
    for value in obj.values():
        if isinstance(value, dict):
            _coerce_numeric_fields(value, depth + 1)
        elif isinstance(value, list):
            for item in value:
                if isinstance(item, dict):
                    _coerce_numeric_fields(item, depth + 1)
```

- [ ] **Step 3: 运行现有测试确认不破坏已有功能**

```bash
cd backend; python -m pytest tests/test_training.py -x -q
```

- [ ] **Step 4: Commit**

```bash
git add backend/contexts/training/_scoring_validation.py
git commit -m "feat: evidence/reason content validation + recursive depth limit"
```

---

### Task 2: `prompts/scoring.py` — 评分提示词重写

**涉及模块：A1, A3**
**说明：** 移除 inline json 范例、冗余 markdown 指令；增强重试提示词

- [ ] **Step 1: 读取并重写 `SCORING_SYSTEM`**

移除：
- "## 评分范例" 整个段落（含 inline json）
- "直接输出 JSON，不要加 markdown 代码块标记"（`response_format=json_object` 已保证）

保留并精简其他部分。system prompt 不应超过原文 2/3 长度。

```python
SCORING_SYSTEM = """你是一位经验丰富的护理教育评估专家，专门评估护理学生的病史采集能力。

{#scoring_criteria#}

## 必须采集到的内容清单（参考）
{#required_inquiries#}

## 评估重点
护理学生的病史采集能力，包括：沟通技能 + 系统问诊 + 临床推理。

## 输出规则

根据对话内容逐项评分，每项 1-3 分：
- 3 分：学生主动、完整地覆盖了该项内容，提问自然、深入
- 2 分：学生部分涉及该项内容，但不完整或不够深入
- 1 分：学生未提及该项内容

**每项必须提供：**
- `evidence`：直接引用对话原文中支持评分的具体证据（至少 10 个汉字）
- `reason`：为什么给这个分数，点出关键得失（至少 5 个汉字）
- 学生未涉及的条目：score=1，evidence="未涉及"

**只输出 JSON，严格遵循以下 schema：**
{#scoring_json_schema#}

## 输出前自检
- total_score 在合理范围内
- 每项都有 id、name、score(1-3)、evidence(≥10字)、reason(≥5字)
- 未涉及的条目 score=1，evidence="未涉及"
"""
```

- [ ] **Step 2: 重写 `SCORING_FEEDBACK_SYSTEM`**

精简锚点相关内容（已经通过 brief criteria 提供）。保留反馈生成逻辑。

```python
SCORING_FEEDBACK_SYSTEM = """你是一位经验丰富的护理教育导师，为护理学生撰写个性化病史采集训练反馈。

## 评分维度参考（概要）
{#scoring_criteria#}

## 必须采集到的内容清单
{#required_inquiries#}

## 反馈要求

生成四项反馈，每项都必须引用对话中的具体行为：

1. **strengths（必填，至少2条）**：学生做得好的具体行为
2. **weaknesses（必填，至少2条）**：需要改进的具体方面
3. **missed_content（必填，至少2条）**：对照必须采集清单，学生漏问的关键信息
4. **suggestions（必填，200-350字）**：个性化改进建议，格式包含肯定+不足+可操作方法

## 输出格式

```json
{
  "strengths": ["具体亮点1", "具体亮点2"],
  "weaknesses": ["具体不足1", "具体不足2"],
  "missed_content": ["漏问信息1", "漏问信息2"],
  "suggestions": "200-350字的个性化建议"
}
```

四项缺一不可。只输出 JSON。"""
```

- [ ] **Step 3: 重写 `SCORING_RETRY_USER`**  

注入具体校验错误：

```python
SCORING_RETRY_USER = (
    "你上一次的输出存在以下问题：\n{validation_errors}\n\n"
    "你上一次的输出：\n```json\n{partial_json}\n```\n\n"
    "请重新输出完整的 JSON，确保每条目的 id、name、score(1-3)、evidence(≥10字)、reason(≥5字) 都完备。"
)
```

注意保留 `{partial_json}` 占位符（已在 `_score_stage` 中使用 `.format()`）。

- [ ] **Step 4: 更新 `_score_stage` 的重试调用**

读取 `score_engine.py:62`，在调用 `SCORING_RETRY_USER.format(partial_json=partial_json)` 之前，调用 `_validate_items_content` 捕获具体错误，注入 `validation_errors`：

```python
item_errors = _validate_items_content(result.get("detail_scores", {}))
validation_msg = "; ".join(item_errors) if item_errors else "字段缺失或不完整"
retry_user = SCORING_RETRY_USER.format(
    partial_json=partial_json,
    validation_errors=validation_msg,
)
```

需要在 `score_engine.py` 顶部引入 `_validate_items_content`：
```python
from ._scoring_validation import (
    _check_feedback_empty, _coerce_numeric_fields, _convert_to_100_scale,
    _merge_feedback, _validate_feedback_fields, _validate_items_content,
    _validate_scoring_essentials, _validate_scoring_result,
)
```

- [ ] **Step 5: 运行测试**

```bash
cd backend; python -m pytest tests/test_training.py -x -q
```

- [ ] **Step 6: Commit**

```bash
git add backend/prompts/scoring.py backend/contexts/training/score_engine.py
git commit -m "feat: rewrite scoring prompts — remove redundant examples, enhance retry with validation errors"
```

---

### Task 3: `static.py` — `build_scoring_criteria` 增加 `level` 参数

**涉及模块：A2**

- [ ] **Step 1: 修改 `build_scoring_criteria` 签名与实现**

```python
def build_scoring_criteria(rubric: dict | None = None, level: str = "full") -> str:
```

`level="full"`：现有行为（含锚点）
`level="brief"`：只输出维度名 + 条目名 + 满分值，省略锚点文本

```python
def build_scoring_criteria(rubric: dict | None = None, level: str = "full") -> str:
    if rubric is None:
        rubric = _get_default_rubric()
    dimensions = rubric.get("dimensions", [])
    raw_max = rubric.get("raw_max", rubric.get("total_max", 57))
    rubric_name = rubric.get("name", "")
    rubric_version = rubric.get("version", "")
    raw_scale = rubric.get("raw_scale", 3)
    display_max = rubric.get("total_max", 100)

    lines = []
    lines.append("## 评分标准版本")
    lines.append(
        f"{rubric_name} v{rubric_version}（原始{raw_max}分制，每项1-{raw_scale}分，系统将自动换算为{display_max}分制）"
    )
    lines.append("")
    lines.append("## 评估维度与条目")
    lines.append("")

    for dim in dimensions:
        dim_name = dim["name"]
        dim_max = dim["max"]
        lines.append(f"### {dim_name}（{len(dim['items'])}项，满分{dim_max}分）")
        if dim.get("description"):
            lines.append(str(dim["description"]))
        lines.append("")

        for i, item in enumerate(dim["items"]):
            if level == "brief":
                lines.append(f"{i+1}. {item['name']}（满分{raw_scale}分）")
            else:
                anchors = item.get("anchors", {})
                anchor_text = " / ".join(f"{k}分: {v}" for k, v in sorted(anchors.items()))
                lines.append(f"{i+1}. {item['name']} — {anchor_text}")

        lines.append("")

    return "\n".join(lines)
```

- [ ] **Step 2: 运行测试**

```bash
cd backend; python -m pytest tests/test_training.py -x -q
```

- [ ] **Step 3: Commit**

```bash
git add backend/infrastructure/prompt/static.py
git commit -m "feat: build_scoring_criteria add level=brief mode for feedback stage"
```

---

### Task 4: `score_engine.py` — feedback 阶段使用 brief criteria + tracker 占位

**涉及模块：A2, D（tracker 占位）**

- [ ] **Step 1: feedback prompt 构建改用 `level="brief"`**

```python
# feedback stage 使用简要评分标准
scoring_criteria_text_brief = build_scoring_criteria(rubric, level="brief")

tmpl_feedback = await pm.get("scoring_feedback")
feedback_system, feedback_user = tmpl_feedback.render_pair(
    scoring_criteria=scoring_criteria_text_brief,
    required_inquiries=required_inquiries_text,
    conversation_text=conversation_text,
)
```

保留原来的 `scoring_criteria_text`（full）不变，给 scoring 阶段使用。

- [ ] **Step 2: `evaluate_training` 签名增加可选的 `tracker` 参数**

```python
async def evaluate_training(
    record_id: int,
    case_data: dict,
    db: Session,
    *,
    pm,
    llm_client: LLMClient,
    tracker=None,  # ScoringProgressTracker | None
) -> Score:
```

在关键位置插入 tracker 调用：

```python
if tracker:
    tracker.start(record_id)
    tracker.update(record_id, "loading", 5, "正在加载对话记录...")

# ... 加载消息后
if tracker:
    tracker.update(record_id, "loading", 5, "对话记录加载完成")

# ... 构建 prompts 后
if tracker:
    tracker.update(record_id, "scoring", 10, "正在评分维度分析...")

# ... LLM calls
scoring_result, feedback_result = await asyncio.gather(scoring_task, feedback_task)

if tracker:
    tracker.update(record_id, "saving", 95, "正在保存评分结果...")

# ... 保存
```

- [ ] **Step 3: `_score_stage` 和 `_feedback_stage` 内部更新 tracker**

在各自的开始和结束处更新进度：

```python
async def _score_stage(messages, record_id, rubric, *, user_id, case_id, log_meta, llm_client, llm_cfg=None, tracker=None):
    if tracker:
        tracker.update(record_id, "scoring", 15, "正在逐项评分分析...")
    # ... LLM call
    if tracker:
        tracker.update(record_id, "scoring", 55, "评分维度分析完成")
    return result
```

```python
async def _feedback_stage(messages, record_id, *, user_id, case_id, log_meta, llm_client, llm_cfg=None, tracker=None):
    if tracker:
        tracker.update(record_id, "feedback", 65, "正在生成反馈建议...")
    # ... LLM call
    if tracker:
        tracker.update(record_id, "feedback", 90, "反馈建议生成完成")
    return result
```

- [ ] **Step 4: 运行测试**

```bash
cd backend; python -m pytest tests/test_training.py -x -q
```

- [ ] **Step 5: Commit**

```bash
git add backend/contexts/training/score_engine.py backend/infrastructure/prompt/static.py
git commit -m "feat: feedback brief criteria + tracker placeholders in score_engine"
```

---

### Task 5: `parsing.py` — detail_scores 解析加固

**涉及模块：B2**

- [ ] **Step 1: 新增 `_extract_json_value` 安全解析函数**

```python
def _extract_json_value(text: str, start: int) -> tuple[dict, int] | None:
    """使用 json.JSONDecoder.raw_decode 从指定位置安全解析一个 JSON 值，限制嵌套深度。"""
    max_depth = 15
    depth = 0
    for i, ch in enumerate(text[start:], start=start):
        if ch == "{":
            depth += 1
            if depth > max_depth:
                return None
        elif ch == "}":
            depth -= 1
            if depth == 0:
                try:
                    decoder = json.JSONDecoder()
                    obj, end = decoder.raw_decode(text, start)
                    return obj, end
                except json.JSONDecodeError:
                    return None
    return None
```

- [ ] **Step 2: 将 `detail_scores` 的正则回退替换为安全解析**

在 `_safe_parse_json` 中，将现有的 detail_scores 正则提取（第 54-70 行）替换为：

```python
elif field == "detail_scores":
    idx = text.find('"detail_scores"')
    if idx != -1:
        colon = text.find(":", idx + 15)
        if colon != -1:
            parsed = _extract_json_value(text, colon + 1)
            if parsed:
                result["detail_scores"] = parsed[0]
```

注意：这仅作为 json.loads 全部失败后的回退路径，正常情况下不会走到这里。

- [ ] **Step 3: 运行测试**

```bash
cd backend; python -m pytest tests/test_training.py -x -q
```

- [ ] **Step 4: Commit**

```bash
git add backend/infrastructure/llm/parsing.py
git commit -m "feat: detail_scores safe JSON extraction in parsing fallback"
```

---

### Task 6: `config.py` — SCORING_TIMEOUT 300→180

- [ ] **Step 1: 修改默认值**

```python
SCORING_TIMEOUT_SECONDS = int(os.getenv("SCORING_TIMEOUT_SECONDS", "180"))
```

- [ ] **Step 2: Commit**

```bash
git add backend/core/config.py
git commit -m "chore: reduce SCORING_TIMEOUT_SECONDS from 300 to 180"
```

---

### Task 7: `scoring_progress.py` — 新建进度跟踪器

**涉及模块：D**
**测试：`backend/tests/test_scoring_progress.py`**

- [ ] **Step 1: 新建 `backend/infrastructure/scoring_progress.py`**

```python
"""In-memory scoring progress tracker with auto-cleanup."""
import time
import logging
from dataclasses import dataclass, field
from typing import Literal

log = logging.getLogger(__name__)

Phase = Literal["loading", "scoring", "feedback", "saving", "completed", "failed"]

@dataclass
class ScoringProgress:
    phase: Phase
    percentage: int
    message: str
    updated_at: float = field(default_factory=time.time)

class ScoringProgressTracker:
    """In-memory store for per-record scoring progress. Thread-safe for asyncio."""

    def __init__(self, ttl: int = 600):
        self._store: dict[int, ScoringProgress] = {}
        self._ttl = ttl

    def start(self, record_id: int) -> None:
        self._store[record_id] = ScoringProgress(phase="loading", percentage=0, message="开始评分")

    def update(self, record_id: int, phase: Phase, pct: int, msg: str) -> None:
        self._store[record_id] = ScoringProgress(
            phase=phase, percentage=pct, message=msg, updated_at=time.time()
        )

    def get(self, record_id: int) -> ScoringProgress | None:
        self._cleanup()
        entry = self._store.get(record_id)
        if entry is None:
            return None
        # Return completed/failed entries for a while, clean expired
        if entry.phase in ("completed", "failed") and time.time() - entry.updated_at > 60:
            self._store.pop(record_id, None)
            return None
        return entry

    def remove(self, record_id: int) -> None:
        self._store.pop(record_id, None)

    def _cleanup(self) -> None:
        now = time.time()
        expired = [rid for rid, p in self._store.items() if now - p.updated_at > self._ttl]
        for rid in expired:
            self._store.pop(rid, None)
            log.debug("Cleaned up stale scoring progress: record_id=%d", rid)
```

- [ ] **Step 2: 写单元测试**

新建 `backend/tests/test_scoring_progress.py`：

```python
import time
from backend.infrastructure.scoring_progress import ScoringProgressTracker

def test_tracker_lifecycle():
    t = ScoringProgressTracker(ttl=600)
    t.start(1)
    assert t.get(1) is not None
    assert t.get(1).phase == "loading"

    t.update(1, "scoring", 30, "正在评分")
    entry = t.get(1)
    assert entry.phase == "scoring"
    assert entry.percentage == 30

    t.remove(1)
    assert t.get(1) is None

def test_tracker_cleanup_expired():
    t = ScoringProgressTracker(ttl=0.1)
    t.start(1)
    assert t.get(1) is not None
    time.sleep(0.2)
    # After TTL, get() triggers cleanup
    assert t.get(1) is None

def test_tracker_completed_stays_shortly():
    t = ScoringProgressTracker(ttl=600)
    t.start(1)
    t.update(1, "completed", 100, "完成")
    assert t.get(1) is not None  # completed stays for 60s
```

- [ ] **Step 3: 运行测试**

```bash
cd backend; python -m pytest tests/test_scoring_progress.py -x -v
```

- [ ] **Step 4: Commit**

```bash
git add backend/infrastructure/scoring_progress.py tests/test_scoring_progress.py
git commit -m "feat: ScoringProgressTracker — in-memory scoring phase tracking"
```

---

### Task 8: `scoring.py` (router) — tracker 生命周期 + `/scoring-status` 返回 progress

**涉及模块：D**

- [ ] **Step 1: `_run_scoring_background` 接入 tracker**

```python
async def _run_scoring_background(
    record_id: int,
    case_data: dict,
    *,
    llm_client: LLMClient,
    pm: PromptManager,
    tracker: ScoringProgressTracker | None = None,
) -> None:
    ...
    try:
        await asyncio.wait_for(
            evaluate_training(
                record_id,
                case_data,
                db,
                pm=pm,
                llm_client=llm_client,
                tracker=tracker,
            ),
            timeout=SCORING_GLOBAL_TIMEOUT,
        )
        record.scoring_status = "completed"
        record.scoring_error = None
        if tracker:
            tracker.update(record_id, "completed", 100, "评分完成")
        db.commit()
    except TimeoutError:
        ...
        if tracker:
            tracker.update(record_id, "failed", 0, "评分超时（超过5分钟）")
    except Exception as e:
        ...
        if tracker:
            tracker.update(record_id, "failed", 0, str(e)[:100])
```

- [ ] **Step 2: `POST /{record_id}/end` 中传递 tracker**

```python
await request.app.state.task_queue.enqueue(
    lambda: _run_scoring_background(
        record_id,
        case.case_data if case else {},
        llm_client=request.app.state.llm_client,
        pm=request.app.state.prompt_manager,
        tracker=request.app.state.scoring_tracker,
    ),
    priority=5,
)
```

同样修改 `retry_scoring` 端点。

- [ ] **Step 3: `GET /{record_id}/scoring-status` 返回 progress**

```python
@router.get("/{record_id}/scoring-status")
def get_scoring_status(...):
    ...
    progress = None
    tracker: ScoringProgressTracker | None = getattr(request.app.state, "scoring_tracker", None)
    if tracker:
        p = tracker.get(record_id)
        if p:
            progress = {
                "phase": p.phase,
                "percentage": p.percentage,
                "message": p.message,
            }

    return {
        "scoring_status": record.scoring_status,
        "scoring_error": record.scoring_error,
        "score": {...} if score else None,
        "progress": progress,
    }
```

注意：需要 import `ScoringProgressTracker`，并从 `fastapi` 引入 `Request` 参数（当前已有 `request: Request`）。

- [ ] **Step 4: 运行测试**

```bash
cd backend; python -m pytest tests/test_training.py -x -q
```

- [ ] **Step 5: Commit**

```bash
git add backend/contexts/training/router/scoring.py
git commit -m "feat: integrate ScoringProgressTracker into scoring endpoints"
```

---

### Task 9: `main.py` — 安装 ScoringProgressTracker

**涉及模块：D**

- [ ] **Step 1: 在 `main.py` 的 app 启动处添加**

```python
from infrastructure.scoring_progress import ScoringProgressTracker

# ... in create_app or lifespan
app.state.scoring_tracker = ScoringProgressTracker()
```

找到 `app.state.llm_client` 或 `app.state.prompt_manager` 初始化的位置（约 main.py 的 app lifespan / startup 事件），在其附近添加。

- [ ] **Step 2: Commit**

```bash
git add backend/main.py
git commit -m "feat: install ScoringProgressTracker in app state"
```

---

### Task 10: `ScoreManager.ts` — 重写（基于后端进度）

**涉及模块：D**

- [ ] **Step 1: 重写 `ScoreManager` 类**

```typescript
// frontend/src/engine/ScoreManager.ts
import { api } from "@/api/axios-instance";
import type { MessageBus, ScoreData, ScoringProgress } from "./types";

export class ScoreManager {
	private recordId: number | null;
	private bus: MessageBus | null;
	private _score: ScoreData | null = null;
	private _progress: ScoringProgress = { phase: null, percentage: 0, message: "" };
	private _polling = false;
	private pollTimer: ReturnType<typeof setInterval> | null = null;
	private listeners: Array<() => void> = [];
	private _visibilityHandler: (() => void) | null = null;

	constructor(recordId: number | null, bus?: MessageBus) {
		this.recordId = recordId;
		this.bus = bus ?? null;
	}

	get score(): ScoreData | null { return this._score; }
	get progress(): ScoringProgress { return this._progress; }
	get polling(): boolean { return this._polling; }

	subscribe(fn: () => void): () => void {
		this.listeners.push(fn);
		return () => { this.listeners = this.listeners.filter((l) => l !== fn); };
	}

	private notify(): void {
		for (const fn of this.listeners) fn();
		if (this.bus && this._score) {
			this.bus.emit("score:ready", this._score);
		}
	}

	async end(): Promise<void> {
		if (!this.recordId) return;
		this._progress = { phase: "loading", percentage: 5, message: "正在结束训练..." };
		this.notify();
		await api.post(`/training/${this.recordId}/end`);
		this._progress = { phase: "loading", percentage: 10, message: "评分已触发，等待后台处理..." };
		this.notify();
		this.startPolling();
	}

	private startPolling(): void {
		if (this._polling || !this.recordId) return;
		this._polling = true;
		let retries = 0;
		const maxRetries = 200; // 1.5s * 200 = 300s

		const poll = async () => {
			if (!this._polling || document.hidden) return;
			if (retries >= maxRetries) {
				this._progress = { phase: "failed", percentage: 0, message: "评分超时" };
				this.stopPolling();
				this.notify();
				return;
			}
			try {
				const res = await api.get(`/training/${this.recordId}/scoring-status`);
				const data = res.data as {
					scoring_status?: string;
					scoring_error?: string | null;
					score?: { total_score?: number } | null;
					progress?: { phase: string; percentage: number; message: string } | null;
				};
				if (data.scoring_status === "failed") {
					this._progress = {
						phase: "failed",
						percentage: 0,
						message: data.scoring_error || "评分失败",
					};
					this.stopPolling();
					this.notify();
					return;
				}
				if (data.score && data.score.total_score !== undefined) {
					this._score = data.score as ScoreData;
					this._progress = { phase: "completed", percentage: 100, message: "评分完成" };
					this.stopPolling();
					this.notify();
					return;
				}
				// 使用后端真实进度
				if (data.progress) {
					this._progress = {
						phase: data.progress.phase as any,
						percentage: data.progress.percentage,
						message: data.progress.message,
					};
				} else {
					// 后端尚未返回 progress 时使用轮询次数估算
					const pct = Math.min(95, 10 + retries * 1.5);
					this._progress = { phase: "processing", percentage: pct, message: "评分处理中..." };
				}
				this.notify();
			} catch {
				const pct = Math.min(95, 10 + retries * 1.5);
				this._progress = { phase: "processing", percentage: pct, message: "评分处理中..." };
				this.notify();
			}
			retries++;
		};

		this.pollTimer = setInterval(poll, 1500);
		this._visibilityHandler = () => { if (!document.hidden) poll(); };
		document.addEventListener("visibilitychange", this._visibilityHandler);
		poll();
	}

	// ... stopPolling, dispose, reset, setRecordId 保持不变
}
```

- [ ] **Step 2: 更新 `types.ts` — 新增 `ScoringProgress` 类型**

```typescript
// frontend/src/engine/types.ts
export type ScorePhase = "loading" | "scoring" | "feedback" | "saving" | "completed" | "failed" | "processing" | null;

export interface ScoringProgress {
	phase: ScorePhase;
	percentage: number;
	message: string;
}
```

- [ ] **Step 3: 运行 TypeScript 编译检查**

```bash
cd frontend; npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/engine/ScoreManager.ts frontend/src/engine/types.ts
git commit -m "refactor: ScoreManager — real backend progress-based polling"
```

---

### Task 11: `ScoringOverlay.tsx` — 重写（真实进度展示）

**涉及模块：D**

- [ ] **Step 1: 重写 `ScoringOverlay`**

```tsx
import { useEffect, useState } from "react";
import type { MessageBus, ScorePhase } from "@/engine/types";

const phaseLabels: Record<string, string> = {
	loading: "正在加载对话记录...",
	scoring: "正在评分维度分析",
	feedback: "正在生成反馈建议",
	saving: "正在保存评分结果...",
	completed: "评分完成 ✓",
	failed: "评分失败",
	processing: "评分处理中...",
};

interface Progress {
	phase: ScorePhase;
	percentage: number;
	message: string;
}

export function ScoringOverlay({
	bus,
	getProgress,
}: {
	bus: MessageBus;
	getProgress: () => Progress;
}) {
	const [visible, setVisible] = useState(false);
	const [progress, setProgress] = useState<Progress>({
		phase: null,
		percentage: 0,
		message: "",
	});

	useEffect(() => {
		const unsub = bus.on("training:ended", () => {
			setVisible(true);
		});
		return unsub;
	}, [bus]);

	// Poll progress from ScoreManager
	useEffect(() => {
		if (!visible) return;
		const id = setInterval(() => {
			const p = getProgress();
			setProgress(p);
			if (p.phase === "completed" || p.phase === "failed") {
				setTimeout(() => setVisible(false), 1500);
			}
		}, 200);
		return () => clearInterval(id);
	}, [visible, getProgress]);

	if (!visible) return null;

	const phaseText = progress.phase
		? phaseLabels[progress.phase] || progress.phase
		: "";

	return (
		<div className="fixed inset-0 z-40 flex flex-col items-center justify-center bg-background/90">
			<p className="mb-4 text-lg font-medium">正在评估训练表现...</p>
			<div className="h-2 w-64 rounded-full bg-muted">
				<div
					className="h-full rounded-full bg-primary transition-all duration-300"
					style={{ width: `${progress.percentage}%` }}
				/>
			</div>
			<p className="mt-2 text-sm text-muted-foreground">
				{phaseText}
				{progress.phase !== "completed" && progress.phase !== "failed"
					? ` (${progress.percentage}%)`
					: ""}
			</p>
			{progress.phase === "failed" && progress.message && (
				<p className="mt-1 text-xs text-red-500">{progress.message}</p>
			)}
		</div>
	);
}
```

- [ ] **Step 2: 更新 `TrainingEngine.tsx` 中 `ScoringOverlay` 的调用**

找到渲染 `<ScoringOverlay>` 的位置，传入 `getProgress`：

```tsx
<ScoringOverlay
	bus={busRef.current}
	getProgress={() => scoreRef.current?.progress ?? { phase: null, percentage: 0, message: "" }}
/>
```

- [ ] **Step 3: 运行 TypeScript 编译检查**

```bash
cd frontend; npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/plugins/scoring-display/ScoringOverlay.tsx frontend/src/engine/TrainingEngine.tsx
git commit -m "refactor: ScoringOverlay — real progress phase display"
```

---

### Task 12: `settlement.py` — 超时自动评分

**涉及模块：E**

- [ ] **Step 1: 修改 `_settle_once` 触发评分**

`_settle_once` 当前接收 `repo`、`emotion_cache`、`initiative_cache`。需要额外接收 `app`（用于访问 `llm_client`、`prompt_manager`、`scoring_tracker`）。

将 `_settle_once` 签名改为异步接收 app_state：

```python
async def _settle_once(repo, *, app_state=None, emotion_cache=None, initiative_cache=None):
    timeout_records = await repo.find_timeout_records()
    ...
    for record in timeout_records:
        try:
            await repo.mark_completed(record.id)
            # 触发评分（若尚未评分）
            if record.scoring_status is None and app_state:
                await _trigger_scoring_if_needed(record.id, app_state)
            ...
```

- [ ] **Step 2: 实现 `_trigger_scoring_if_needed`**

```python
async def _trigger_scoring_if_needed(record_id: int, app_state) -> None:
    """在结算循环中触发评分，与正常评分使用相同逻辑。"""
    from core.database import SessionLocal
    from models import TrainingRecord, Case
    from contexts.training.router.scoring import _run_scoring_background

    db = SessionLocal()
    try:
        record = db.query(TrainingRecord).filter(TrainingRecord.id == record_id).first()
        if not record or record.scoring_status is not None:
            return
        case = db.query(Case).filter(Case.id == record.case_id).first()
        case_data = case.case_data if case else {}

        await _run_scoring_background(
            record_id,
            case_data,
            llm_client=app_state.llm_client,
            pm=app_state.prompt_manager,
            tracker=getattr(app_state, "scoring_tracker", None),
        )
    finally:
        db.close()
```

注意：创建一个独立的 DB session，避免与 repo 的 session 冲突。

- [ ] **Step 3: 更新 `settlement_loop` 调用点**

在 `main.py` 中：

```python
asyncio.create_task(
    settlement_loop(
        repo,
        interval=CLEANUP_INTERVAL_SECONDS,
        app_state=app.state,
        emotion_cache=app.state.emotion_cache,
        initiative_cache=app.state.initiative_cache,
    )
)
```

- [ ] **Step 4: Commit**

```bash
git add backend/infrastructure/settlement.py backend/main.py
git commit -m "feat: auto-trigger scoring on timed-out sessions in settlement loop"
```

---

### 验证清单

实施完成后，按以下顺序验证：

```bash
# 1. 后端单元测试
cd backend; python -m pytest tests/test_scoring_progress.py tests/test_training.py -v

# 2. 后端 lint + type check
cd backend; python -m ruff check; .venv/Scripts/ty.exe check

# 3. 前端编译
cd frontend; npx tsc --noEmit; npx biome check

# 4. 手动测试：发起一次训练，结束训练，观察进度阶段变化
```

---

## Spec Coverage 对照

| Spec 要求 | 对应 Task |
|-----------|-----------|
| A1: scoring prompt 移除冗余示例 | Task 2 |
| A2: feedback brief criteria | Task 3, 4 |
| A3: 重试提示词注入校验错误 | Task 2 |
| A4: evidence/reason 代码校验 | Task 1 |
| B1: 递归深度限制 | Task 1 |
| B2: detail_scores 解析加固 | Task 5 |
| C: SCORING_TIMEOUT 300→180 | Task 6 |
| D: ScoringProgressTracker | Task 7 |
| D: score_engine tracker 注入 | Task 4 |
| D: scoring.py tracker 集成 + progress 返回 | Task 8 |
| D: main.py tracker 安装 | Task 9 |
| D: ScoreManager 重写 | Task 10 |
| D: ScoringOverlay 重写 | Task 11 |
| E: settlement 自动评分 | Task 12 |
