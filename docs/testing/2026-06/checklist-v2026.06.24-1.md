# v2026.06.24-1 测试清单

**版本**: v2026.06.23-4 → v2026.06.24-1
**环境**: https://test.205716.xyz

### 1. 产品介绍页（/showcase）整体浏览
**操作**: 在浏览器打开 https://test.205716.xyz/showcase → 从上往下滚动浏览整个页面
**预期**: 页面从上到下依次展示：顶栏、首屏大标题、概览数字条、六大亮点卡片、工程化底座说明、技术栈跑马灯、底部 CTA 区域；各区块无重叠、无错位、无明显闪烁

### 2. 首屏 Hero 区域
**操作**: 进入 /showcase → 观察首屏区域（未滚动前）
**预期**: 页面标题「把 LLM 做成可教学、可评估的虚拟患者」可见；副标题显示训练面板/评分维度/情绪状态等数字卖点；「进入系统」按钮可点击，点击后跳转到 /login

### 3. 概览数字条滚动计数
**操作**: 进入 /showcase → 向下滚动至概览数字条区域
**预期**: 数字条中的统计数字（如训练面板数、评分维度等）在进入视野时从 0 逐次增加到目标值；数字动画流畅无卡顿

### 4. 六大亮点滚动转场
**操作**: 进入 /showcase → 向下滚动至「六大亮点」区域 → 继续缓慢滚动
**预期**: 每个亮点卡片在接近视野顶部时固定停留（sticky），下一个卡片上推覆盖上一个；每个卡片包含图标、标题、说明文字和截图占位图；滚动过程无明显抖动

### 5. 技术栈跑马灯与底部 CTA
**操作**: 进入 /showcase → 向下滚动至页面底部
**预期**: 技术栈区域有横向滚动的图标与名称（如 React、Python 等）；底部「进入系统」按钮可点击跳转 /login；页面底部有版权信息

### 6. 页面响应式与导航顶栏
**操作**: 在 /showcase 页面，调整浏览器窗口宽度从宽屏逐渐缩小至手机宽度
**预期**: 页面布局自适应调整，文字不溢出、图片不超出屏幕；顶栏在滚动后保持固定顶部、「进入系统」按钮始终可见
