export type { TTSProvider } from "./types";

// ── 以下为预留代码，当前未启用 ──
// 浏览器 TTS（当前默认）：import { createBrowserTTS } from "./browser-tts"
// 火山 TTS（未来切换）：import { createVolcengineTTS } from "./volcengine-tts"
