# 虚拟患者训练系统交付说明

交付时间：2026-05-21

## 目录内容

- `backend/`：FastAPI 后端源码、病例 JSON、SQLite 初始数据库、依赖清单
- `frontend/`：React/Vite 前端源码、`package-lock.json`、最新 `dist/` 构建产物
- `docs/`：系统架构、API、数据库、前端、LLM、启动指南、商业化打磨交接文档

## 未包含内容

- `frontend/node_modules/`：体积过大，请在目标机器执行 `npm install`
- Python `__pycache__/` 和 `.pyc` 缓存文件
- 真实 DeepSeek API Key：出于安全原因不写入代码包

## 启动方式

### 1. 配置 DeepSeek API Key

在启动后端前，在 PowerShell 中设置：

```powershell
$env:DEEPSEEK_API_KEY="你的DeepSeek API Key"
$env:SECRET_KEY="生产环境请换成强随机字符串"
```

### 2. 启动后端

```powershell
cd backend
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
python -m uvicorn main:app --host 127.0.0.1 --port 8000
```

### 3. 启动前端

```powershell
cd frontend
npm install
npm.cmd run dev
```

访问：`http://localhost:3000`

## 默认账号

- 教师：admin / admin123
- 学生：student1 / 123456
- 学生：student2 / 123456
- 学生：student3 / 123456
- 学生：student4 / 123456
- 学生：student5 / 123456

## 当前版本说明

当前交付版本为 `v1.6-polish`：已完成 Dashboard 真实反馈/统计接入、快速问答跳转、57分评分口径统一、主要页面图标与样式打磨、安全配置改造。

后续继续开发请优先阅读：

- `docs/README.md`
- `docs/06-dev-log.md`
- `docs/08-polish-handoff.md`
